{
  "format_version": "1.21.0",
  "animation_controllers": {
    "controller.animation.fauno_quebrado.general": {
      "initial_state": "idle",
      "states": {
        "idle": {
          "animations": ["idle"],
          "transitions": [
            {
              "move": "query.is_moving && query.attack_time <= 0"
            },
            {
              "charge": "query.has_target && query.attack_time > 0"
            },
            {
              "death": "query.is_dead"
            }
          ]
        },
        "move": {
          "animations": ["move"],
          "transitions": [
            {
              "idle": "!query.is_moving"
            },
            {
              "charge": "query.has_target && query.attack_time > 0"
            },
            {
              "death": "query.is_dead"
            }
          ]
        },
        "charge": {
          "animations": ["charge"],
          "transitions": [
            {
              "idle": "query.attack_time <= 0 || !query.has_target"
            },
            {
              "death": "query.is_dead"
            }
          ]
        },
        "death": {
          "animations": ["death"],
          "transitions": []
        }
      }
    }
  }
}